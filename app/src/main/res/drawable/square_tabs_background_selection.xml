<?xml version="1.0" encoding="UTF-8"?>
<shape xmlns:android="http://schemas.android.com/apk/res/android"
    android:shape="rectangle">
    <gradient
        android:endColor="@color/white"
        android:startColor="@color/white" />
</shape>

    <!--
    <?xml version="1.0" encoding="utf-8"?>
    <selector xmlns:android="http://schemas.android.com/apk/res/android">
        <item android:state_pressed="true">
            <shape android:shape="rectangle">
                <corners android:radius="8dip" />
                <stroke android:width="1dip" android:color="@color/tab_color" />
                <gradient android:angle="-90" android:endColor="@color/transparent" android:startColor="@color/tab_color" />
            </shape>
        </item>
        <item android:state_focused="true">
            <shape android:shape="rectangle">
                <corners android:radius="8dip" />
                <stroke android:width="1dip" android:color="@color/tab_color" />
                <solid android:color="@color/tab_color" />
            </shape>
        </item>
        <item>
            <shape android:shape="rectangle">
                <corners android:radius="8dip" />
                <stroke android:width="1dip" android:color="@color/tab_color" />
                <gradient android:angle="-90" android:endColor="@color/tab_color" android:startColor="@color/tab_color" />
            </shape>
        </item>
    </selector>-->
